# failsafe: autologin root no console serial (ttyAML0, 115200 8N1).
# Sobe um shell sem pedir senha assim que o systemd alcanca o getty.
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin root --keep-baud 115200,38400,9600 %I $TERM
