# A simples existencia deste arquivo sinaliza ao apply-flavor.sh que o flavor
# e "failsafe": NAO instalar o servico tx9-firstboot e NAO adicionar configuracao
# de rede (o networkd do tarball base ainda sobe). Conteudo ignorado.
